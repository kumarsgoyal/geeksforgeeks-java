class GfG {
    int transitionPoint(int arr[], int n) {
        int flag=0;
        for(int i = 0; i < n; i++) {
            if(arr[i] == 1) {
                flag=i;
                break;
            }
        }
        return (flag);
    }
}
