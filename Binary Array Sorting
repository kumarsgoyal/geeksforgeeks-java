  
class Solution {
    //Function to sort the binary array.
    static void binSort(int A[], int N) {
        int j = 0;
        
        for(int i = 0; i < N; i++) {
            if(A[i] == 0) {
                // swap
                int temp = A[i];
                A[i] = A[j];
                A[j] = temp;
                j++;
            }
        }
        
        /**************
        * No need to print the array
        * ************/
    }
}

