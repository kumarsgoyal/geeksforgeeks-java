class Solution {

    int sumOfDependencies(ArrayList<ArrayList<Integer>> adj, int V) {
        int sum = 0;
        
        for(int i = 0; i < adj.size(); i++) {
            sum = sum + adj.get(i).size();
        }
        
        return sum;
    }
    
};
