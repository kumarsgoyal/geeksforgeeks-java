class GfG {

    public static int findMax(Height arr[], int n) {
        int result = 0;
        
        for(int i = 0; i < n; i++) {
            result = Math.max(result, arr[i].feet * 12 + arr[i].inches);
        }
       
        return result;
    }
    
}
